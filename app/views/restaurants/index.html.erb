<div class="container mt-5">
    <h1>Restaurants</h1>

    <ul>
    <% @restaurants.each do |resto| %>
        <li>
            <%= link_to  resto.name, restaurant_path(resto) %>
        </li>
    <% end %>
    </ul>

    <%= button_to "Create a restaurant", new_restaurant_path, method: :get %>

</div>